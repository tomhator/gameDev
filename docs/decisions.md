# 결정 로그

대화 원문은 남기지 않는다. 남기는 것은 **맥락 → 근거 → 결과** 세 줄이다.
새 항목은 **맨 위에** 붙인다. 지난 항목은 고치지 않는다 — 뒤집혔으면 새 항목에 "앞 항목을 뒤집음"이라고 적는다.
규칙은 CLAUDE.md 규약의 "결정 로그" 절. 티켓 안에서 끝난 결정은 티켓 댓글이 원본이고, 여기엔 세 줄 + `#번호`만 둔다.

```
## YYYY-MM-DD · 제목 (#티켓)
- 맥락: 무엇을 하다가 이 판단이 필요해졌나
- 근거: 무엇을 보고 그렇게 정했나 (버린 대안 포함)
- 결과: 무엇이 달라졌나 / 다음에 무엇을 하나
```

---

<!-- 새 항목은 이 줄 바로 아래에 -->

## 2026-09-11 · cheoma → rainy-day, 별도 저장소에서 진행 (#37)
- 맥락: 첫 쁘띠 사이클 소재로 #37(처마 투 처마)을 고른 뒤, 이 저장소의 `cheoma/` 폴더에서 할지 저장소를 나눌지 정해야 했다.
- 근거: 결정권자가 프로젝트명을 `rainy-day`로 정하고 `tomhator/rainy-day`를 팠다. 분리하면 gameDev의 WIP 상한(`stage:build` 1, #36이 점유)이 사이클을 막지 않는다. 폴더 동거(jigsaw-snap 방식)는 버렸다.
- 결과: 사이클 티켓·코드는 전부 rainy-day. gameDev는 아이디어 대장·키트·노하우 허브로만 남고 #37은 닫았다. rainy-day에 키트(`tools/ticket-kit/apply.sh`)와 `docs/templates/` 3종을 붙이고 `CLAUDE_CODE_OAUTH_TOKEN` 시크릿을 넣는 게 남은 수작업.

## 2026-09-11 · drill-guard·football-xcom 보류, 상세 기획 대신 토이 (#37)
- 맥락: 새 사이클을 시작하려면 규약 4조("전작 출시 전 다음 시작 금지")와 충돌하고, "상세 기획을 진행해도 되나"는 질문이 같이 왔다.
- 근거: 둘 다 미출시로 멈춰 있어 "전작 출시"가 영영 오지 않는다 — 완료(공개 URL) 하나를 먼저 만드는 쪽이 낫다. 기획 문서는 `docs/knowhow.md` §1-3의 사망 패턴(문서가 코드를 앞지른 4개 프로젝트 전부 종료)이고, §1-4의 유일하게 옳았던 피벗은 프로토를 하루 굴려 보고 나왔다.
- 결과: 둘을 README에 **보류**로 명시(4조의 해소가 아니라 예외). 기획 문서는 쓰지 않고 #37 다듬기 댓글의 완료 조건 7줄이 기획의 전부. 9/13(일) S1 토이 + 같은 날 3바퀴 플레이 + 노트 3줄, 올림 9/23·공개 9/25는 그대로.

## 2026-09-10 · 대화 맥락은 원문이 아니라 세 줄로 남긴다 (#31)
- 맥락: 티켓 댓글 밖(터미널·클로드 앱 세션)에서 오간 논의를 어떻게 관리하느냐는 질문. 지금 규약이 붙잡는 채널은 티켓 댓글과 STATUS.md 둘뿐이고, 세션 대화는 끝나면 사라진다.
- 근거: 원문 transcript를 쌓으면 다음 세션이 읽을 양이 통제 불능이 되고, 로컬 memory는 환경마다 있고 없어서(Action 세션엔 없다) 진실의 소스로 못 쓴다. 필요한 건 "왜 그렇게 됐나"이지 대화 자체가 아니다. STATUS.md는 덮어쓰는 인수인계 메모라 누적 기록으로 부적합.
- 결과: `docs/decisions.md` 신설 — 맥락→근거→결과 세 줄을 맨 위에 append. 세션 시작에 최근 10개를 읽고 세션 끝에 적는다. 규약에 "결정 로그" 절 추가.

## 2026-09-10 · 티켓에 단계 6개와 동시 진행 제한을 붙인다 (#31)
- 맥락: 티켓이 "무엇인가"(종류 라벨)만 있고 "어디까지 왔나"가 없어서, 여러 개가 동시에 반쯤 굴러가다 아무것도 안 끝났다.
- 근거: #20 부검이 지목한 실패 모드는 크리티컬 패스를 아트가 막는 것과 동시 진행 과다. 상한을 숫자로 못박아야 "자리가 없다"가 판단이 아니라 사실이 된다. 8세션 사이클에 셋 이상은 안 끝난다는 관측치.
- 결과: `stage:idea→spec→build→test→art→verify` 6단계 + WIP 상한(구현 1 · 아트 1 · 기획~검수 합계 3 · 아이디어 5). 초과 시 `wip-over` 라벨과 경고 댓글. 키트 v2.0.0. `bug`/`tuning`은 기획·아트 생략, `decision`/`question`/`playtest`는 단계 없음.

## 2026-09-10 · 완료의 정의를 "공개 URL + 타인 플레이"로 바꾼다 (#20)
- 맥락: 14개 시작, 0개 출시. 그레이박스 완성을 완료로 세고 있었다(`docs/knowhow.md` §1).
- 근거: 지표가 "진척"이면 무한히 진척할 수 있다. 외부 플레이어 1명이 유일하게 위조 불가능한 게이트.
- 결과: 완료 = 공개 URL + 나 아닌 사람 1명의 플레이 + 피드백 3줄. 플레이 노트 게이트, 백로그 상한 5, 14일·8세션 사이클, 아트 8장 규칙이 규약 "개발 루프" 절로 들어감.
